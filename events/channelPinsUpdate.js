module.exports = function(client) {
	// client.on('channelPinsUpdate', (channel, time) => {
	// 	if (!options.disabled) {
	// 		if (channel.id == options.channel.shitposting) {
	// 			channel.messages.fetchPinned().then(messages => {
	// 				messages.each(message => {
	// 					if (message.id != options.message.pins) {
	// 						pinMessage(message);
	// 						message.unpin();
	// 					}
	// 				})
	// 			}).catch(console.error);
	// 		}
	// 	}
	// });
}