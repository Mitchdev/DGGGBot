exports.name = ['pin']
exports.permission = 'mod'
exports.handler = function(message) {
	// var args = message.content.replace('!pin ', '').split(' ');
	// if (message.reference) {
	// 	client.channels.resolve(message.reference.channelID).messages.fetch(message.reference.messageID).then(msg => {
	// 		pinMessage(msg);
	// 	}).catch(console.error);
	// } else if (args.length = 1) {
	// 	if (parseInt(args[0])) {
	// 		client.channels.resolve(message.channel.id).messages.fetch(message.content.replace('!pin ', '')).then(msg => {
	// 			pinMessage(msg);
	// 		}).catch(console.error);
	// 	}
	// }
}